{
	"name": "amico",
	"version": "0.0.1",
	"description": "A node js chat app",
	"main": "backend/dist/server.js",
	"scripts": {
		"prestart": "npm run bundle:back && npm run bundle:front",
		"start": "cd backend && npm run start",
		"deploy": "cd backend && now",
		"bundle": "npm run bundle:back && npm run bundle:front",
		"bundle:front": "cd frontend && npm install && npm run build",
		"bundle:back": "cd backend && npm install && npm run build"
	},
	"dependencies": {
	},
	"engines": {
		"node": "12.x"
	},
	"keywords": [
		"node",
		"hyperdev",
		"express",
		"chat"
	],
	"license": "MIT"
}
